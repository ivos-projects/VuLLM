from setuptools import setup

setup(
    name="vullm",
    author="Oscar Andersson, Ivar Nelson",
    version="1.0",
    # "src" should be the name of your project's root directory
    packages=["src"],
)